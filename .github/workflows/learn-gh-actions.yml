name: learn-github-action
run-name: ${{ github.actor }} is learning Github Actions
on: 
  push:
  workflow_dispatch:
    
jobs:
  check-bats-version:
    runs-on: ubuntu-latest
    steps: 
    - run: echo date
    
